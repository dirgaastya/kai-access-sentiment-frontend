import wordcloudData from './wordcloud_data.json';
export const NEGATIF_CONSTANT = Object.entries(wordcloudData.negatif);
export const POSITIF_CONSTANT = Object.entries(wordcloudData.positif);
export const NETRAL_CONSTANT = Object.entries(wordcloudData.netral);
